<%# El n/N comparte renglón con el código: los dos son cortos y los dos van
    grandes. Apilados, la columna izquierda dejaba la sucursal en "SAN PED…". %>
<% if etiqueta_plantilla.visible?("fraccion") %>
  <span style="font-size:var(--fs-fraccion); font-weight:bold; margin-left:auto; white-space:nowrap;"><%= etiqueta_fraccion(paquete) %></span>
<% end %>
