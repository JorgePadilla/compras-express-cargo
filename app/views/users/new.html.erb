<%= render PageHeaderComponent.new(title: "Nuevo Usuario") do |header|
  header.with_back do
    render ButtonComponent.new(variant: :ghost, href: users_path, icon: "arrow-left", shortcut: "F2") do
      "Volver"
    end
  end
end %>

<div class="bg-white rounded-lg shadow overflow-hidden">
  <div class="p-6">
    <%= render "form", user: @user %>
  </div>
</div>
