<%= render PageHeaderComponent.new(title: "Nueva Nota de Debito", subtitle: @venta ? "Venta: #{@venta.numero}" : nil) %>

<% if @venta.nil? %>
  <div class="bg-white rounded-lg shadow p-6">
    <p class="text-sm text-gray-700 mb-4">Para crear una nota de debito, ingresa primero a la Venta y usa el boton "Crear Nota de Debito".</p>
    <%= link_to "Ver Ventas", ventas_path, class: "px-4 py-2 bg-cec-navy text-white rounded-lg text-sm font-medium" %>
  </div>
<% else %>
  <%= render "form", form_url: notas_debito_path, form_method: :post %>
<% end %>
